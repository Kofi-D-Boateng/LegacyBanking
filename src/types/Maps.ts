export type DateAmountHash = {
  [date: string]: number;
};

export type DateAmountType = {
  date: string;
  amount: number;
  type: string;
};

export type MonthsMap = {
  [date: number]: string;
};
