import { FC, Fragment } from "react";
const Partners: FC<{
  classes: {
    readonly [key: string]: string;
  };
  PARTNERS: {
    key: number;
    img: string;
  }[];
}> = () => {
  return <></>;
};

export default Partners;
