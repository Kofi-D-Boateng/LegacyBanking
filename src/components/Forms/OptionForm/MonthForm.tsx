import { SelectChangeEvent } from "@mui/material";
import { FC } from "react";

const MonthForm: FC<{
  monthHandler: (e: SelectChangeEvent<string>) => void;
}> = () => {
  return <></>;
};

export default MonthForm;
